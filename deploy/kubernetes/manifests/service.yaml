apiVersion: v1
kind: Service
metadata:
  name: kapa-dashboard
  labels:
    app: kapa-dashboard
spec:
  type: ClusterIP
  selector:
    app: kapa-dashboard
  ports:
    - name: http
      port: 8080
      targetPort: http
