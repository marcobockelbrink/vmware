# Secrets NIE ins Repo committen — dieses Beispiel lokal befüllen oder
# per "kubectl create secret" anlegen. / Never commit secrets — fill this
# example locally or use "kubectl create secret":
#   kubectl create secret generic kapa-dashboard-secrets \
#     --from-literal=ARIA_PASSWORD='DAS-ARIA-PASSWORT'
apiVersion: v1
kind: Secret
metadata:
  name: kapa-dashboard-secrets
type: Opaque
stringData:
  ARIA_PASSWORD: "HIER-EINTRAGEN"
  # SMTP_PASSWORD: ""
  # BACKUP_PASSWORD: ""
