apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kapa.fullname" . }}-config
  labels:
    {{- include "kapa.labels" . | nindent 4 }}
data:
  kapa.ini: |
    {{- .Values.kapaIni | nindent 4 }}
