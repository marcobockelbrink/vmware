---
- name: RPM-Quelle prüfen
  ansible.builtin.assert:
    that: kapa_rpm | length > 0
    fail_msg: "kapa_rpm muss gesetzt sein (Pfad, URL oder Paketname des RPM)."

- name: kapa-dashboard installieren
  ansible.builtin.dnf:
    name: "{{ kapa_rpm }}"
    state: present
    disable_gpg_check: true   # bei unsigniertem internem RPM; besser: RPM signieren
  notify: restart kapa-dashboard

- name: Konfigurationsdatei /etc/kapa/kapa.ini schreiben
  ansible.builtin.template:
    src: kapa.ini.j2
    dest: /etc/kapa/kapa.ini
    owner: root
    group: kapa
    mode: "0640"
  notify: restart kapa-dashboard

# Geheimnisse als eigene .pass-Dateien (root:kapa, 0640 – der Dienst liest sie
# direkt, es gibt kein systemd LoadCredential mehr). Pfade stehen in der INI.
- name: Aria-Passwort ablegen (/etc/kapa/aria.pass)
  ansible.builtin.copy:
    content: "{{ kapa_aria_password }}\n"
    dest: /etc/kapa/aria.pass
    owner: root
    group: kapa
    mode: "0640"
  no_log: true
  when: kapa_aria_password | length > 0
  notify: restart kapa-dashboard

- name: AD-Service-Konto-Passwort ablegen (/etc/kapa/ad_bind.pass)
  ansible.builtin.copy:
    content: "{{ kapa_ad_bind_password }}\n"
    dest: /etc/kapa/ad_bind.pass
    owner: root
    group: kapa
    mode: "0640"
  no_log: true
  when: kapa_ad_bind_password | default('') | length > 0
  notify: restart kapa-dashboard

- name: SMTP-Passwort ablegen (/etc/kapa/smtp.pass)
  ansible.builtin.copy:
    content: "{{ kapa_smtp_password }}\n"
    dest: /etc/kapa/smtp.pass
    owner: root
    group: kapa
    mode: "0640"
  no_log: true
  when: kapa_smtp_password | default('') | length > 0
  notify: restart kapa-dashboard

- name: SELinux – nginx darf zum Dienst proxien
  ansible.posix.seboolean:
    name: httpd_can_network_connect
    state: true
    persistent: true
  when:
    - ansible_selinux is defined
    - ansible_selinux.status == "enabled"

- name: nginx-Snippet einbinden
  ansible.builtin.copy:
    src: /etc/kapa/nginx-kapa.conf.sample
    dest: /etc/nginx/conf.d/kapa.conf
    remote_src: true
    owner: root
    group: root
    mode: "0644"
  when: kapa_manage_nginx | bool
  notify: reload nginx

- name: Dienst aktivieren und starten
  ansible.builtin.systemd:
    name: kapa-dashboard
    enabled: "{{ kapa_service_enabled | bool }}"
    state: "{{ kapa_service_state }}"
    daemon_reload: true
