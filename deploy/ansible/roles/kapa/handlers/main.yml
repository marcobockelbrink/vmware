---
- name: restart kapa-dashboard
  ansible.builtin.systemd:
    name: kapa-dashboard
    state: restarted
    daemon_reload: true

- name: reload nginx
  ansible.builtin.systemd:
    name: nginx
    state: reloaded
