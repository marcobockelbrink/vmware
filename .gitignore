# Laufzeitdaten (lokaler Datencontainer, gehört komplett nicht ins Repo)
data/
kapa_cache.json
kapa_reservierungen.json
kapa_rollen.json
kapa_dashboard.html
# Generierte Demo-Seite ist ein Build-Artefakt (jederzeit erzeugbar mit
#   python3 aria_kapa.py --sample --output kapa_dashboard_demo.html
# ) und blähte den Diff bei jedem Release auf.
kapa_dashboard_demo.html
*.json
!package.json

# Python
__pycache__/
*.pyc

# macOS
.DS_Store
