# Laufzeitdaten (lokaler Datencontainer, gehört komplett nicht ins Repo)
data/
kapa_cache.json
kapa_reservierungen.json
kapa_rollen.json
kapa_dashboard.html
# Die generierte Demo-Seite kapa_dashboard_demo.html ist bewusst IM Repo
# (klickbare Demo ohne Installation). Neu erzeugen bei Releases mit:
#   python3 aria_kapa.py --sample --output kapa_dashboard_demo.html
*.json
!package.json

# Python
__pycache__/
*.pyc

# macOS
.DS_Store
